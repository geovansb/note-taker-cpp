# Plano de Implementação para T4.1 e T4.2

## Objetivo
Continuar o desenvolvimento do note-taker-bar, restaurando o menu completo (T4.1) e implementando o EventTap para hotkey global (T4.2), baseado no estado atual onde M1-M3 estão completos e T4.1 está parcialmente implementado.

## Estado Atual
- Menu bar app básico funcionando com ícone e menu mínimo ("Quit").
- Arquivos `src/app_delegate.mm` e `src/app_delegate.h` existem.
- `src/event_tap.h` e `src/event_tap.mm` não existem.
- CMakeLists.txt configurado para note-taker-bar, mas sem event_tap.mm ou framework Carbon.

## Próximos Passos

### Tarefa 1: T4.1 Cleanup — Restaurar Menu Completo em src/app_delegate.mm
- **Descrição**: Substituir o menu mínimo por um esqueleto completo com itens desabilitados, exceto Quit.
- **Subtarefas**:
  - Modificar `setupStatusItem` para construir o menu completo conforme especificado (● Idle, separadores, Hold ⌥ Right Option, ▶ Start Recording, ■ Stop Recording, 📂 Open Notes Folder, Quit).
  - Adicionar tags (1-4) nos itens apropriados.
  - Itens funcionais devem ser desabilitados (enabled = NO), exceto Quit.
- **Arquivo**: `src/app_delegate.mm`
- **Critério de Aceite**: Menu mostra esqueleto completo; Start/Stop/Open desabilitados; Quit funciona.

### Tarefa 2: Adicionar Stub Selectors em app_delegate.mm
- **Descrição**: Implementar métodos no-op para startRecording, stopRecording e openNotesFolder.
- **Subtarefas**:
  - Adicionar implementações vazias dos três métodos em `app_delegate.mm`.
- **Arquivo**: `src/app_delegate.mm`

### Tarefa 3: Declarar Stub Selectors em app_delegate.h
- **Descrição**: Adicionar declarações dos métodos stub na interface.
- **Subtarefas**:
  - Incluir declarações para - (void)startRecording:(id)sender; etc. na interface @interface.
- **Arquivo**: `src/app_delegate.h`

### Tarefa 4: T4.2 — Criar src/event_tap.h
- **Descrição**: Definir a interface da classe EventTap.
- **Subtarefas**:
  - Criar arquivo com #pragma once, include <functional>, e classe EventTap com construtor, destrutor, setHotkey, start, stop, e Impl privado.
- **Arquivo**: `src/event_tap.h`
- **Critério de Aceite**: Arquivo compila sem erros.

### Tarefa 5: Implementar src/event_tap.mm
- **Descrição**: Implementar EventTap com CGEventTap, thread dedicada, etc.
- **Subtarefas**:
  - Implementar CGEventTapCreate com kCGSessionEventTap, listen-only, filtro keyDown/Up.
  - Checar AXIsProcessTrustedWithOptions antes de iniciar.
  - Thread com CFRunLoopRun, stop() desabilita tap e para loop.
  - Impl PIMPL com ponteiros para tap, run_loop, thread.
- **Arquivo**: `src/event_tap.mm`
- **Critério de Aceite**: Classe compila; start retorna false se Accessibility negado.

### Tarefa 6: Wiring Temporário em app_delegate.mm
- **Descrição**: Instanciar EventTap, conectar callbacks para debug, iniciar/parar em lifecycle.
- **Subtarefas**:
  - Adicionar ivar _eventTap em @implementation.
  - Em applicationDidFinishLaunching: chamar start com callbacks que imprimem "debug: hotkey DOWN/UP".
  - Em applicationWillTerminate: chamar stop.
- **Arquivo**: `src/app_delegate.mm`

### Tarefa 7: Atualizar CMakeLists.txt
- **Descrição**: Adicionar event_tap.mm às sources e linkar Carbon.
- **Subtarefas**:
  - Incluir src/event_tap.mm em add_executable(note-taker-bar ...).
  - Adicionar "-framework Carbon" em target_link_libraries(note-taker-bar ...).
- **Arquivo**: `CMakeLists.txt`

## Critérios Gerais de Aceite
- Compilação bem-sucedida: `cmake --build build --parallel`.
- App lança sem crash.
- Para T4.2: Pressionar Right Option → stderr "debug: hotkey DOWN"; soltar → "debug: hotkey UP"; outras teclas ignoradas.
- Sem crash se Accessibility negado (apenas warning).

## Dependências
- macOS com Accessibility e Menu Bar autorizados.
- Reconstruir após mudanças para atualizar assinatura ad-hoc.

## Questões em Aberto
- Confirmar se ARC está habilitado para event_tap.mm (já está para todo OBJCXX).
- Verificar comportamento em macOS Tahoe 26+.